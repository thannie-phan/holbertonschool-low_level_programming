#include "main.h"

/**
 * main - Entry point
 *
 * Description: Fill in on github after
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
{
	_putchar(letter);
	letter++;
}
return (0);
}

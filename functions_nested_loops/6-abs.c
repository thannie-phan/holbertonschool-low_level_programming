#include "main.h"

/**
 * _abs - computes absolutes value of an integer or rm - if found
 * @n: the integer to check
 * Return: absolutes value of integer
 *
 *
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (n * -1);
	}
	else
	{
	return (n);
	}
}

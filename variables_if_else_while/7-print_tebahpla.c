#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the size of various types
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char number;

	for (number = 'a'; letter <= 'z'; letter++)
		putchar (letter);
	for (letter = '0'; letter <= '9'; letter++)
		putchar (letter);
	putchar ('\n');
	return (0);
}
